
# Here I am going to use local variables
# Local variables means the variables which are present inside a function

def local_variable() :
    a = 10                               # Local variable
    print("Accessing inside the method : ", a)

# print("Accessing outside the method : ", a)    | Here we can't access a local variable outside the method 
                                                 # Because it's scope is upto that method only
