
"""Tuple is a data structure in python and it maintains the insertion order means we can returieve the element by using index value
   and it is immutable means we can not modify it's element, it also accepts duplicate element and also it accepts heterogeneous data,
   for creating a tuple we need to use () braces
"""

nums = (1, 4, 6, 8)

# nums[0] = 2   # We can't change it's element

for i in nums:
    print(i)